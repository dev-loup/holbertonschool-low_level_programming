#include "holberton.h"

/**
 * add - prints the sum between two values
 * @a: value 1
 * @b: value 2
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int a, int b)
{
	int r;

		r = (a + b);
	return (r);
}
